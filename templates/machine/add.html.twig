{% extends 'base.html.twig' %}

{% block body %}
    <div class="col d-flex justify-content-center">
        <div >
            <img class="d-flex justify-content-center"
                 src="{{ asset('./images/computer.jpeg') }}"/>
            <div class="d-flex justify-content-center">
                <p class="card-text">This will be a NEW machine.</p>
            </div>
            <div class="d-flex justify-content-center">
                {{ form_start(form) }}
                <div class="mt-2">
                    {{ form_row(form.cpu) }}
                </div>
                <div class="mt-2">
                    {{ form_row(form.ram) }}
                </div>
                <input class=" btn btn-success mt-3" type="submit" name="submit" value="Create">
                {{ form_start(form) }}
            </div>
        </div>
    </div>


{% endblock %}

